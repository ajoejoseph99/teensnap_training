# Project Requirements
1. The system must support JWT-based authentication.
2. We need to integrate with a modern email provider (e.g., SendGrid or Resend).
3. All dates must be handled using the 'date-fns' library.

*Exercise: Ask Copilot to search the web for the latest 'date-fns' 
version and refactor the calculator.ts to use it for 'added_at' timestamps.*
